{% macro feed_post(title, date, url) %}
  <article>
    <time datetime="{{ date.toISOString() }}">{{ date | date('YYYY.MM.DD') }}</time>
    <a href="{{ url }}">{{ title }}</a>
  </article>
{% endmacro %}
